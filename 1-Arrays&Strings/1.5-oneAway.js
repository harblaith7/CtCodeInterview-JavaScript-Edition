const oneAway = (string1, string2) => {
    const lengthStr1 = string1.length;
    const lengthStr2 = string2.length;

    // IF THERE IS A MISMATCH IN LENGTH, WE CAN OMIT REPLACEMENT
    // IF THE LENGTH DISCREPANCY IS MORE THAN 1, THEN WE RETURN FALSE 
    if(lengthStr1 )


}



// pales --- pale